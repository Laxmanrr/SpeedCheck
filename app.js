document.getElementById('startTest').addEventListener('click', function() {
    // Placeholder action for starting the speed test
    document.getElementById('speedValue').textContent = Math.floor(Math.random() * 100); // Simulating speed
});
